/* The entry point of the application */
ENTRY_POINT = el0_main ;
/* The address where everything is loaded. The entry point
 * must be placed there. */
BOOT_ADDR   = 0x800000 ;
/* This constant is not used here */
STACK_SIZE  = 0 ;

